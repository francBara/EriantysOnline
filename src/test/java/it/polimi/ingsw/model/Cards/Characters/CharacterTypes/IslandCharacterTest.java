package it.polimi.ingsw.model.Cards.Characters.CharacterTypes;

import it.polimi.ingsw.model.Cards.Characters.Exceptions.IslandRequiredException;
import it.polimi.ingsw.model.Map.Island;
import it.polimi.ingsw.model.Player.Player;
import junit.framework.TestCase;

public class IslandCharacterTest extends TestCase {

    public void testIslandCharacter() {
        /*
        IslandCharacter islandCharacter = new IslandCharacter(
            "",
            "",
            1,
            (Island island) -> {

            }
        );

        assertNull(islandCharacter.getStudents());

        assertEquals(1, islandCharacter.getCost());
        assertTrue(islandCharacter.requiresIsland());
        assertFalse(islandCharacter.requiresFaction());

        assertNotNull(islandCharacter.getIslandRequirement());
        assertNull(islandCharacter.getFactionRequirement());

        boolean catched = false;

        try {
            islandCharacter.useEffect(new Player(""));
            fail();
        } catch(IslandRequiredException ignored) {
            catched = true;
        }

        assertTrue(catched);

        assertEquals(1, islandCharacter.getCost());

        islandCharacter.getIslandRequirement().setIsland(new Island());

        islandCharacter.useEffect(new Player(""));

        assertEquals(2, islandCharacter.getCost());

         */
    }
}