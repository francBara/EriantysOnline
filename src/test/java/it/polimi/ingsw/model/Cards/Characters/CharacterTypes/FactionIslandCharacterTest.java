package it.polimi.ingsw.model.Cards.Characters.CharacterTypes;

import it.polimi.ingsw.model.Faction;
import it.polimi.ingsw.model.Map.Island;
import it.polimi.ingsw.model.Students;
import junit.framework.TestCase;

public class FactionIslandCharacterTest extends TestCase {

    public void testFactionIslandCharacter() {
/*
        Students cardStudents = new Students();

        cardStudents.add(Faction.PinkFairies);

        for (int i = 0; i < 3; i++) {
            cardStudents.add(Faction.BlueUnicorns);
        }

        FactionIslandCharacter factionIslandCharacter = new FactionIslandCharacter(
            "",
            "",
            2,
            cardStudents,
            (Faction faction, Island island) -> {

            },
            (Faction faction, Students students) -> {

            }
        );

        assertEquals(2, factionIslandCharacter.getCost());
        assertEquals(cardStudents, factionIslandCharacter.getStudents());

        assertTrue(factionIslandCharacter.requiresFaction());
        assertTrue(factionIslandCharacter.requiresIsland());

        factionIslandCharacter.increaseCost();

        assertEquals(3, factionIslandCharacter.getCost());
        */
    }


}